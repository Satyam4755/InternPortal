<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Intern Dashboard</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <h2>Intern Dashboard</h2>
    <div class="card">
      <p><strong>Name:</strong> <span><%= intern.name %></span></p>
      <p><strong>Referral Code:</strong> <span><%= intern.referralCode %></span></p>
      <p><strong>Total Donations:</strong> ₹<span><%= intern.totalDonations %></span></p>
      <div class="rewards">
        <strong>Rewards:</strong>
        <ul>
          <% intern.rewards.forEach(reward => { %>
            <li><%= reward %></li>
          <% }) %>
        </ul>
      </div>
    </div>
    <a href="/leaderboard" class="btn">View Leaderboard</a>
  </div>
</body>
</html>